cmake_minimum_required(VERSION 3.13)  # CMake version check
project (libecc)
include(libecc.cmake)

# Add executable target with source files listed in SOURCE_FILES variable
#add_executable(simple_example ${SOURCE_FILES})
